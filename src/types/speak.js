import identity from 'lodash/identity';

export const tag = 'speak';

export const schema = {
    title: 'speak',
    description: '',

    type: 'object',
    required: [],
    properties: {

    },
};

export const transform = identity;
