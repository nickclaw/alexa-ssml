import identity from 'lodash/identity';

export const tag = 'p';

export const schema = {
    "title": "p",
    "description": "",

    "type": "object",
    "required": [],
    "properties": {

    }
}

export const transform = identity;
