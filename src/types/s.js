import identity from 'lodash/identity';

export const tag = 's';

export const schema = {
    title: 's',
    description: '',

    type: 'object',
    required: [],
    properties: {

    },
};

export const transform = identity;
